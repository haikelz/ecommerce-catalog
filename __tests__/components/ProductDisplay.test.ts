import ProductDisplay from "../../src/components/ProductDisplay.vue";

describe("Product Display component test", () => {
  it("Should render the component", () => {
    expect(ProductDisplay).toBeDefined();
  });
});
